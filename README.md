# Chrome host app

Entrypoint into cloud, host app

npm i

---

env='DEVELOPMENT":

npm run start

---

env='PRODUCTION'

npm run build

build folder contain all static files

npm run server => to run node js server on port 8080 that return static from build folder
